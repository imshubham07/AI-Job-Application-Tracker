# Client (React + Vite)

Run instructions for the complete project are documented in the root guide:

- [../README.md](../README.md)

Quick start (client only):

```bash
npm install
npm run dev -- --host
```

Default local URL:

- `http://localhost:5173`
